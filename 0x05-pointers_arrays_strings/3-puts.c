#include "main.h"
#include <stdio.h>

/**
 * _puts - it prints characters to the standout output.
 *
 * @str: the character string to be printed.
 *
 */

void _puts(char *str)
{
	printf("%s\n", str);
}
